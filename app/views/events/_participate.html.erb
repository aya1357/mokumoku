<% if @user.gender == "女性" && event.only_woman == "only_woman" || @user.gender == "男性" && event.only_woman == "not_set" || @user.gender == "女性" && event.only_woman == "not_set"%>
  <%= link_to 'このもくもく会に参加する',
              event_attendance_url(@event),
              class: 'btn btn-primary',
              method: :post,
              data: { confirm: '申し込みます' }
  %>
<% end %>
