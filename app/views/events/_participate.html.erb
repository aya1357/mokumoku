<% if current_user.woman? && event.only_woman? || current_user.man? && event.only_woman == false || current_user.woman? && event.only_woman == false %>
  <%= link_to 'このもくもく会に参加する',
              event_attendance_url(@event),
              class: 'btn btn-primary',
              method: :post,
              data: { confirm: '申し込みます' }
  %>
<% end %>
